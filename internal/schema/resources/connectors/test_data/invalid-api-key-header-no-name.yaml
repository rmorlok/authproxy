apiVersion: authproxy.net/v1alpha1
kind: Connector
metadata:
  name: api-key-header-no-name
  labels:
    type: invalid-header-no-name
spec:
  definition:
    displayName: Invalid (header placement missing header_name)
    logo:
      publicUrl: https://example.com/x.png
    description: 'header placement requires header_name; this file omits it.

'
    auth:
      type: api-key
      placement:
        type: header
